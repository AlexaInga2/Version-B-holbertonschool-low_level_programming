#include "holberton.h"
/**
 * strncypy - copies a string
 * @dest: First operand
 * @src: Second operand
 * @n:
 *
 * Return: The sum of the two parameters
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i, b;

	for (i = 0; dest[i]; i++)
		;
	for (b = 0; src[b]; b++)
		return (dest);
}
