#include "holberton.h"
/**
 * print_line - print "_"
 * @n: the integer being compared
 * Return: Always 0.
 */
void print_line(int n)
{
	int b;

	for (b = 0; b < n; b++)
	{
		if (n > 0)
			_putchar('\n');
	}
	_putchar('_');
}
