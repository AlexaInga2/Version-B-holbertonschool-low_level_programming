#include "holberton.h"
/**
 * more_numbers- prints numbers 0-14 10x
 */
void more_numbers(void)
{
	char r = 0, a;

	while (r < 10)
	{
		a = '0';
		while (a > 14)
		{
			_putchar(a);
			a++;
		}
		r++;
		_putchar('\n');
	}
}
