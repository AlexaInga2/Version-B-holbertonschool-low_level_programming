#include <stdio.h>
/**
 * main- entry point
 *
 * Return: always 0 (success)
*/
int main(void)
{
        char n = 'a';
        while (n <= z)
        {
		if ((n != 'q') && (n != 'e'))
		{
			putchar('n');
                	n++;
        	}
	}
	putchar('\n');
        return (0);
}
