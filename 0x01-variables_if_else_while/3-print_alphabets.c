nclude <stdio.h>
/**
 * main- entry point
 *
 * Return: always 0 (success)
*/
int main(void)
{
        char az = 'a';
        while ( az <= z )
        {
                putchar ('az');
                az++;
        }
	char az = 'A'
	while (az <= Z )
	{
		putchar ('az')
		az++
        }
	putchar ('\n');
        return (0);
}
