#include <stdio.h>
/**
 * main- entry point
 *
 * Return: always 0 (success)
*/
int main(void)
{
	char az = 'a';

	while (az <= z)
	{
		putchar ('az');
		az++;
	}
	char AZ = 'A'

	while (AZ <= Z)
	{
		putchar ('az')
		az++
	}
	putchar ('\n');
	return (0);
}
