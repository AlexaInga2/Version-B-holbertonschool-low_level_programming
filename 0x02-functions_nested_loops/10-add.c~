#include "holberton.h"
#include <stdio.h>
/**
 * add - adds to integers
 * @a: first interger to add
 * @b: second integer to add
 * @c: sum of both
 *
 * Return: the result of adding both a & b intergers
 */
int add(int a, int b)
{
	return (a + b);
}
